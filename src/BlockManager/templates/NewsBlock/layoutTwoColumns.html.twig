
<div class="row">
{% for news in news_list %}
    {% if loop.first %}
        <div class="col-md-6">
                    {% if news.image %}
                    <a href="{{ path(news_path, {'slug': news.urlName}) }}" class=""><img src="{% path news.image, 'big' %}" class="img-thumbnail"></a>
                    {% endif %}
                    <h3 class="title">
                        <a href="{{ path(news_path, {'slug': news.urlName}) }}" class="">
                            {{ news.title }}
                        </a>
                    </h3>
                    {% if data.showDate %}
                    <span class="text-muted">{{ news.startPublishAt|date('l, d F Y @ H:i') }}</span>
                    {% endif %}
                    <p>{{ news.lead }}</p>
                    {% if news.author and data.showAuthor %}
                    <p class="text-muted">by <b>{{ news.author }}</b></p>
                    {% endif %}
                    <p><a href="{{ path(news_path, {'slug': news.urlName}) }}">{{ data.textReadMore }} &raquo;</a></p>
        </div>
        <div class="col-md-6">
    {% else %}
            {% if data.showDate %}
            <span class="text-muted">{{ news.startPublishAt|date('l, d F Y @ H:i') }}</span>
            {% endif %}
            {% if news.author and data.showAuthor %}
            <span class="text-muted"> by <b>{{ news.author }}</b></span>
            {% endif %}
            <p><b>{{ news.title }}</b> <a href="{{ path(news_path, {'slug': news.urlName}) }}">{{ data.textReadMore }} &raquo;</a></p>
            {% if not loop.last %}
                <hr>
            {% endif %}
    {% endif %}
    {% if loop.last %}
        </div>
    {% endif %}
{% endfor %}
</div>